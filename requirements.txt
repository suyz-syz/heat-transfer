# ============================================================
# 完整依赖（计算核心 + API 服务 + Kivy 移动端）
# 安装：pip install -r requirements.txt
# 注：Docker 镜像不安装 Kivy，使用 requirements-server.txt 保持轻量。
# ============================================================

# ---- 计算核心 ----
numpy>=1.26,<3

# ---- API 服务（FastAPI / Uvicorn）----
fastapi>=0.110,<1
uvicorn[standard]>=0.29
pydantic>=2.6

# ---- Kivy 跨平台移动端（桌面调试 / Android 打包需要）----
# Android 打包时依赖以 buildozer.spec 的 requirements 为准
kivy>=2.3.0

# ---- 开发与测试 ----
pytest>=8.0
