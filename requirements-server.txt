# Docker 镜像精简依赖（计算核心 kiln_ht/calc.py 零第三方依赖，仅使用 Python 标准库）
# 同时包含 FastAPI API 服务与 Streamlit Web GUI
# 注：不安装 pyarrow（约 85MB）——app.py 的表格已改用 st.markdown 渲染，
#     st.table/st.dataframe 会触发 streamlit 延迟导入 pyarrow，已规避
fastapi>=0.110,<1
uvicorn>=0.29
pydantic>=2.6
streamlit>=1.32
plotly>=5.20
supervisor>=4.2
